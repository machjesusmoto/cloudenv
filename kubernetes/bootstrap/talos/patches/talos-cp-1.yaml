# Node-specific patch for talos-cp-1
# Apply via: talosctl apply-config --config-patch @patches/talos-cp-1.yaml

machine:
  network:
    hostname: talos-cp-1
    interfaces:
      - interface: eth0
        addresses:
          - 10.0.0.11/24
