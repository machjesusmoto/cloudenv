# Node-specific patch for talos-cp-2
# Apply via: talosctl apply-config --config-patch @patches/talos-cp-2.yaml

machine:
  network:
    hostname: talos-cp-2
    interfaces:
      - interface: eth0
        addresses:
          - 10.0.0.12/24
