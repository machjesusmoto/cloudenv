# Monitoring Namespace (Placeholder)
# STATUS: DEFERRED - Phase 7 is a placeholder for future implementation
#
# This namespace will host the kube-prometheus-stack when deployed.
# See kubernetes/core/monitoring/REQUIREMENTS.md for planned components.
#
# Apply when ready:
#   kubectl apply -f monitoring.yaml

apiVersion: v1
kind: Namespace
metadata:
  name: monitoring
  labels:
    app.kubernetes.io/name: monitoring
    app.kubernetes.io/component: observability
    # Pod Security Standards - privileged required for node-exporter
    pod-security.kubernetes.io/enforce: privileged
    pod-security.kubernetes.io/audit: privileged
    pod-security.kubernetes.io/warn: privileged
